# Backend service environment variables
# Copy to .env and adjust as needed.

# Root directory for cached content and index (absolute or relative path)
CONTENT_ROOT=./data/content

# TTL for cached metadata in seconds (default 86400 = 24 hours)
CACHE_TTL=86400
